<?php 
	
	class User extends Eloquent {
		//public static $timestamps = false;	

		/*
		//Grab the user's id from the Auth session
					$user_id = Auth::user()->id;



					//get the users data
					$user = DB::table('users')->where('id', '=', $user_id);
					*/
	}